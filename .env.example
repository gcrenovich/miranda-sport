# Puerto en el que corre el servidor local
PORT=3000

# Cadena de conexión a tu base de datos MongoDB Atlas (Online)
# Reemplaza <username>, <password> y <cluster> con tus datos reales
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/miranda_sport?retryWrites=true&w=majority
